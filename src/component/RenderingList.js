/*export default function RenderingList(){

};

return(
    <div>
        <h2>List of Fruits</h2>
        <ul>
            {fruits.map((item,index)=>(
                <li key={index}>{item}</li>
                <div key={index}>
                    <p>
                        {item.name}
                    </p>
                </div>
            ))}
        </ul>
    </div>
)*/